# Graph

Core graph assembly and dependency-impact analysis live here.

- `graph_builder.py` orchestrates extraction and summarization
- `blast_radius.py` computes reverse-dependency blast radius results
