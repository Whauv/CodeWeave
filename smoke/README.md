# Smoke Tests

Playwright browser smoke and interaction tests live here.

- `ui-smoke.spec.js` covers shell, scan flow, persistence, and evolution mode
- `ui-interactions.spec.js` covers blast UI, exports, and chat rendering
- `fixtures/` contains tiny repos used by the browser tests
